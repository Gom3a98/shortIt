var assert = require('assert');
describe('Array', function() {
  describe('#indexOf()', function() {
    it('should value when the value is present', function() {
      assert.equal([1, 2, 3].indexOf(2), [1, 2, 3].indexOf(2));
    });
  });
});